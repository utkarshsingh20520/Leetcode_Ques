class Solution {
public:
    int largestAltitude(vector<int>& gain) {
       int k=0,max=0;
      for(int i=0;i<gain.size();i++){
         k+=gain[i];
         if(k>max)
             max=k;
      } 
      return max;  
    }
};
